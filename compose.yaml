services:
  gamja:
    image: ghcr.io/ploos-as/gamja:latest
    container_name: gamja
    restart: unless-stopped
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
